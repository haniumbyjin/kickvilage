<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="Admin">
 
	<insert id="join" parameterType="Admin">
		INSERT INTO admin (adminId, adminPw, salt) VALUES ( #{adminId},#{adminPw} , #{salt})
	</insert>
	<select id="getSalt" parameterType="String" resultType="String">
		SELECT salt FROM admin WHERE adminId = #{adminId}
	</select>
	<select id="login" parameterType="Admin" resultType="Int">
		SELECT COUNT(adminId) FROM admin WHERE adminId = #{adminId} AND adminPw = #{adminPw}
	</select>
 </mapper> 